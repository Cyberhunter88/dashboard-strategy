# Finding Discovery Report

## Scope

Repository-wide Codex Security standard scan for `C:\Users\admin\Documents\Claude-Projects\HA-dashboard-strategy`, focused on the current root checkout. Local `.worktrees/`, existing `security-scans/`, media/docs, and generated `dist/` bundles were excluded from deep source review except for release-hygiene and source-map/secret checks.

## Worklist

- `rank_input.csv`: generated by Codex Security worklist helper with 254 source-like rows. It included local ignored worktrees, so it was not used directly as the final deep-review scope.
- `deep_review_input.csv`: curated to the 40 current root runtime/build files in `src/`, `package.json`, `hacs.json`, TypeScript/Webpack config, and ESLint config.
- Subagents reviewed 40/40 deep-review rows in full, with parent-led global sink, secret, and dependency searches.

## Candidates

No raw security candidates were promoted.

## Main Rejected Candidate Families

- DOM injection: `unsafeHTML` receives only bundled static translation strings; `innerHTML` assignments clear private containers with an empty string; Lit text/attribute bindings handle registry metadata.
- Unsafe YAML: `js-yaml` parses administrator/operator Lovelace YAML into config objects. This is intended HA dashboard capability, not anonymous or lower-privileged input.
- Wrong HA action target: generated `perform-action` configs target visible registry-derived `light`, `cover`, and `lock` entity arrays with fixed service names.
- Release/dependency issue: `npm audit --json` and `npm audit --audit-level=low` reported zero vulnerabilities. Production Webpack uses fixed entry name, content-hashed chunks, `output.clean`, and HACS public path.
- Secret exposure: source/config search found no credentials, tokens, API keys, or private keys outside benign identifier names.
